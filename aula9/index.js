// String, number, undefined, nullm, boolean

const nome = 'Breno'; // string
const nome1 = "Breno"; // string
const nome2 = `Breno`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = nao aponta para lugar nenhum na memória
const sobrenomeAluno = null; // null = nao aponta para lugar nenhum na memória
const aprovado = true; // boolean = true or false (lógico)

let a = 2;
const b = a;
console.log(a, b);

a = 3;
console.log(a, b);